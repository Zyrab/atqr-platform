{
  "name": "qr-studio",
  "version": "0.0.1",
  "scripts": {
    "build": "node build.js",
    "deploy": "pnpm build && firebase deploy"
  },
  "type": "module",
  "dependencies": {
    "@zyrab/domo": "^1.3.0",
    "firebase": "^12.6.0",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "dotenv": "^17.2.3",
    "esbuild": "^0.27.0"
  }
}
